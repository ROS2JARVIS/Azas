# Legacy Tools

Temporary holding area for old scripts while docs and teammate workflows are
migrated.

Do not add new files here. Move a legacy script only after either:
- a replacement exists under `pick/`, `perception/`, `checks/`, or `smoke/`, or
- the script is proven unused and can be deleted in a separate cleanup commit.
